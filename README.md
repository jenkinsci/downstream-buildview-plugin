Attention:  -Dhudson.upstreamCulprits=true should be enabled to work this plugin properly
Per-build fingerprinting must be set up - you can use "builds-chain-fingerprinter" plugin to easily do this.
